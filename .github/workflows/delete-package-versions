- name: Delete old images
  uses: snok/container-retention-policy@v2
  with:
    image-names: my-image-name
    cut-off: A week ago UTC
    keep-at-least: 1
    account-type: personal # or 'org'
    token: ${{ secrets.GITHUB_TOKEN }}
